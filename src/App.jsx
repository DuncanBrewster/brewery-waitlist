import React from 'react'

export default function App() {
  return (
    <div style={{ fontFamily: 'system-ui, -apple-system, Segoe UI, Roboto, "Helvetica Neue", Arial', padding: 16 }}>
      <header>
        <h1>Brewery Waitlist</h1>
        <p>Fetching breweries from Open Brewery DB next.</p>
      </header>

      <main>
        <div role="status">App scaffold created — next step: add API hook and components.</div>
      </main>
    </div>
  )
}
